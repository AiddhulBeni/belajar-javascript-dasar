let data = 70;

switch (data) {
  case 90:
    document.writeln(`<p>Nilai kamu adalah : ${data}</p>`);
    break;
  case 80:
    document.writeln(`<p>Nilai kamu adalah : ${data}</p>`);
    break;
  default:
    document.writeln(`<p>Nilai kamu adalah : ${data}</p>`);
}

//for loop
const pet = ["dog", "cat", "horse", "chicken", "snack"];

for (let i = 0; i < pet.length; i++) { 
        document.writeln(`<p>${pet[i]}</p>`)
} 
