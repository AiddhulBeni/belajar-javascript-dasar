let data = 90;

switch(data) {
    case 90:
        document.writeln(`<p>Nilai kamu adalah : ${data}</p>`);
    break;
    case 80:
        document.writeln(`<p>Nilai kamu adalah : ${data}</p>`);
    break;
    default :
        document.writeln(`<p>Nilai kamu bukan : 90`);
}